#!groovy

def dockerImage = ''

pipeline {

  environment {
  }

  agent {
    label 'docker'
  }

  stages{
    stage('Checkout'){
      steps{
        checkout scm
      }
    }
  }

  post {
    cleanup{
      script{
        echo "deleting directory"
        deleteDir()
      }
    }
  }
}