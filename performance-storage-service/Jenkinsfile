#!groovy

def dockerImage = ''

pipeline {
  environment {
    REGISTRY = "cmudb/performance-storage-service"
    REGISTRY_CREDENTIAL = 'cmudb-dockerhub'
    VERSION = "1.0.0"
    TIMESCALE_IMAGE = 'timescaledb:latest-pg12'
  }
  agent {
    label 'docker'
  }
  stages{
    stage('Checkout'){
      steps{
        checkout scm
      }
    }

    stage('Build'){
      steps{
        dir('performance-storage-service'){
          script{
            dockerImage = docker.build REGISTRY + ":${VERSION}"
          }
        }
      }
    }

    stage('Test'){
      steps{
        script{
          dir('performance-storage-service'){
            docker.withRegistry('', REGISTRY_CREDENTIAL) {
              sh "docker pull ${TIMESCALE_IMAGE}"
              docker.image(TIMESCALE_IMAGE).withRun('-e "POSTGRES_PASSWORD=password" -p 5432:5432') { c ->
                /* Wait until timescale service is up */
                sh """
                  until psql -h 127.0.0.1 -U postgres -d postgres -c "select 1" > /dev/null 2>&1; do
                  echo "Waiting for postgres server"
                  sleep 1
                done"""
              }
            }
            sh "docker run -e SECRET_KEY=shhh --entrypoint python ${REGISTRY}:${VERSION} manage.py jenkins --enable-coverage"
          }
        }
      }
    }

    stage('Publish'){
      when{
        branch 'test'
      }
      steps{
        script{
          dir('performance-storage-service'){
            docker.withRegistry('', REGISTRY_CREDENTIAL) {
                dockerImage.push("latest")
                dockerImage.push("${VERSION}")
            }
          }
        }
      }
    }
  }

  post {
    cleanup{
      script{
        if (env.BRANCH_NAME == 'test') {
          echo "removing docker image"
          sh "docker rmi ${REGISTRY}:${VERSION}"
        }
        sh "docker rmi ${TIMESCALE_IMAGE}"
        echo "deleting directory"
        deleteDir()
      }
    }
  }
}
