---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: grafana
  labels:
    app: grafana
    env: test
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: grafana
      env: test
  template:
    metadata:
      labels:
        app: grafana
        env: test
    spec:
      containers:
        - name: grafana
          image: "{{ grafana_container_image }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          # TODO: I need to do something about the SECRET_KEY but I'm not sure what
