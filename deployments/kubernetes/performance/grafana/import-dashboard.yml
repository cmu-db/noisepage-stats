---
- name: Add grafana data source
  vars:
    dir_json_model_repo: "{{inventory_dir}}"
  grafana_dashboard:
    grafana_url: "http://incrudibles-testing.db.pdl.cmu.edu/grafana"
    url_username: "admin"
    url_password: "admin"
    state: present
    message: Updated by ansible
    overwrite: yes
    path: "{{dir_json_model_repo}}/nightly.json"